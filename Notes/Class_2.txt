<!-- STRING CREATION
- Single Quote (One Liner)
- Double Quote (One Liner) 
- Triple Quote (Multi Liner)

Purpose: When you want to print a very specific data.

During print(double_quote), the data that is double quote will be printed.

If double_quote = walaowei
And triple_quote = OMG

And, print(double_quote)

terminal will print only 'walaowei'

Tips: Preferably, don't use a single quote because it might be hard to pair with aposthrope.

STRING INDEXING ANND SLICING

len(): Length - give total amount length (character) in integer
strip90: remove whitespace (front and back)
upper(): UPPERCASE
lower(): lowercase
title{}: Titlecase
replace(): ("old","new")

name= "bob the builder"

print(len(name))
print(name.strip())
print(name.upper())
print(name.lower())
print(name.title())
print(name.replace("bob", "jane"))

Purpose: It gives formatting to the character

STRING FORMATING

Types:
f-string -> Best because can read right away
str.format()
%-formatting

name = "John Doe"
age = 30

message_1 = f"My name is {name} and I am {age} years old." # f-strings
message_2 = "My name is {} and I am {} years old".format(name, age) # str.format ()
message_3 = "My name is %s and I am %d years old." % (name, age) # %-formatting

print(message_1)
print(message_2)
print(message_3)